<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_privacy_policy.xml — Privacy Policy screen
    =====================================================
    Structure:
      ScrollView (root)             — makes long text scrollable
        LinearLayout (vertical)     — stacks back button, title, and body
          ImageButton (backButton)  — calls finish()
          TextView   (tvTitle)      — "Privacy Policy" heading
          TextView   (tvBody)       — full text loaded from res/raw/privacy_policy.txt
-->

<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="40dp">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginTop="48dp"
            android:layout_marginBottom="8dp"
            android:background="@null"
            android:contentDescription="Back"
            android:scaleType="centerInside"
            android:src="@drawable/arrow_left"
            android:tint="@android:color/black" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="24dp"
            android:text="@string/privacy_policy"
            android:textColor="@color/textPrimary"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingMultiplier="1.4"
            android:textColor="@color/textPrimary"
            android:textSize="14sp" />

    </LinearLayout>

</ScrollView>
